# Optional. With a key, /signin and /signup use Clerk email accounts. Without one, demo accounts.
VITE_CLERK_PUBLISHABLE_KEY=

# Server-side only. Set in Netlify environment variables, never VITE_. Powers résumé rewrite suggestions at /api/suggest.
ANTHROPIC_API_KEY=
